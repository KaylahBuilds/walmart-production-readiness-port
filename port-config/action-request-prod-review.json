{
  "identifier": "request_prod_review",
  "title": "Request Production Readiness Review",
  "description": "Request a production readiness review for your service. Updates the service entity and notifies the platform team.",
  "trigger": {
    "type": "self-service",
    "operation": "DAY-2",
    "blueprintIdentifier": "service",
    "userInputs": {
      "properties": {
        "targetLevel": {
          "type": "string",
          "title": "Target Level",
          "description": "The production readiness level you are targeting",
          "enum": [
            "Bronze",
            "Silver",
            "Gold"
          ]
        },
        "notes": {
          "type": "string",
          "title": "Notes",
          "description": "Any additional context for the platform team reviewing your service"
        }
      },
      "required": [
        "targetLevel"
      ]
    }
  },
  "invocationMethod": {
    "type": "UPSERT_ENTITY",
    "blueprintIdentifier": "service",
    "mapping": {
      "identifier": "{{ .entity.identifier }}",
      "properties": {
        "reviewRequested": true,
        "targetLevel": "{{ .inputs.targetLevel }}",
        "reviewNotes": "{{ .inputs.notes }}"
      }
    }
  }
}
